# Local development (.env)
DATABASE_URL="file:./prisma/dev.db"
AUTH_SECRET="generate-via-openssl-rand-base64-32"
RESEND_API_KEY=""
EMAIL_FROM="AI Lab <onboarding@resend.dev>"

# Production (used by docker-compose on the server)
# DATABASE_URL is set inside the container — you don't need it here.
# AUTH_SECRET="<openssl rand -base64 32>"
# AUTH_URL="https://ailab.example.edu"
